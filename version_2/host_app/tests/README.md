# Host Test Scaffold

Planned tests will cover shared golden vectors, arbitrary serial-read
fragmentation, concatenated records, corrupt candidates, boot garbage,
one-outstanding-command reply matching, interleaved asynchronous `\DAT` blocks,
and byte-exact ADC capture.

No test framework or runtime dependency is selected by this scaffold.
